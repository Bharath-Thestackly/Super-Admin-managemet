spring:
  application:
    name: platform-admin-service

  datasource:
    url: jdbc:postgresql://localhost:5432/Cloud_Platform
    username: postgres
    password: Bhagi@1125
    driver-class-name: org.postgresql.Driver

  flyway:
    baseline-on-migrate: true
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    open-in-view: false

server:
  port: 8081

management:
  endpoints:
    web:
      exposure:
        include: health,info

  endpoint:
    health:
      show-details: always
